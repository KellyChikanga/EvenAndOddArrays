let evenArray = []
let oddArray = []

function EvenAndOdd(numsArray){
    for (let i = 0; i < numsArray.length; i++) {
        if (numsArray[i] % 2 === 0) {
            evenArray.push(numsArray[i]);
        } else oddArray.push(numsArray[i]);
    } return sorted = [evenArray, oddArray]
}
